---
import { BRAND_MARK_SRC, ROUTES } from '~/config/site';

type NavLink = { label: string; href: string; external?: boolean };
const navLinks: NavLink[] = [
  { label: 'Index', href: ROUTES.projects },
  { label: 'Contact', href: 'mailto:kontakt@tyndfed.dk', external: true },
];
---

<header class="site-header" data-site-header>
  <div class="shell site-header-inner">
    <a href={ROUTES.home} class="brand" aria-label="Tyndfed — back to home">
      <img src={BRAND_MARK_SRC} alt="" width="28" height="28" class="brand-mark" />
      <span class="brand-name">Tyndfed</span>
    </a>

    <nav class="site-nav" aria-label="Primary">
      <ul>
        {navLinks.map((link) => (
          <li>
            <a href={link.href}>
              {link.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>

<style>
  .site-header {
    z-index: 100;
    width: 100%;
  }

  .site-header-inner {
    display: grid;
    grid-template-columns: 1fr auto;
    align-items: center;
    gap: 24px;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: var(--text-primary);
    width: fit-content;
  }

  .brand-mark {
    width: 28px;
    height: 28px;
    object-fit: contain;
  }

  .brand-name {
    font-family: var(--font-sans);
    font-style: normal;
    font-weight: 650;
    font-size: 22px;
    letter-spacing: 0;
  }

  .site-nav ul {
    list-style: none;
    display: inline-flex;
    align-items: center;
    gap: 20px;
  }

  .site-nav a {
    font-family: var(--font-mono);
    font-size: 11px;
    text-transform: uppercase;
    color: var(--text-secondary);
    padding: 7px 0;
    border: none;
    background: transparent;
    transition: color var(--duration-fast) ease;
  }

  .site-nav a:hover {
    color: var(--accent-strong);
  }

  @media (max-width: 480px) {
    .site-header-inner {
      gap: 12px;
    }

    .brand-name {
      font-size: 18px;
    }

    .site-nav ul {
      gap: 12px;
    }
  }
</style>
